apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: access-dev-env
  namespace: wael-ns
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector: {}
          namespaceSelector:
            matchLabels:
              env: dev
